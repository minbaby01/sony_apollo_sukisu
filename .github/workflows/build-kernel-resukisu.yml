name: Build Kernel - ReSukiSU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CCACHE_COMPILERCHECK: "%compiler% -dumpmachine; %compiler% -dumpversion"
      CCACHE_NOHASHDIR: "true"
      CCACHE_HARDLINK: "true"
      CCACHE_DIR: /home/runner/.ccache_${{ matrix.devices.name }}
      CCACHE_MAXSIZE: 8G
    strategy:
      fail-fast: false
      matrix:
        devices:
          - name: xz2c_dcm
            config: tama_apollo_kddi_defconfig
          - name: xz2c
            config: tama_apollo_defconfig
    name: Build Kernel for ${{ matrix.devices.name }}
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Install Package
        run: sudo apt update && sudo apt install -y p7zip-full git ccache

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: /home/runner/.ccache_${{ matrix.devices.name }}
          key: ccache-${{ runner.os }}-${{ github.ref }}-${{ matrix.devices.name }}
          restore-keys: |
            ccache-${{ runner.os }}-${{ matrix.devices.name }}-
            ccache-${{ runner.os }}-

      - uses: actions/checkout@v5

      - name: Get Kernel Source Code
        uses: actions/checkout@v5
        with:
          repository: LineageOS/android_kernel_sony_sdm845
          ref: lineage-22.2
          path: kernel
      
      - name: Setup Cross-compile toolchain
        run: |
          mkdir -p toolchain
          cd toolchain
          wget -q https://github.com/Mandi-Sa/clang/releases/download/amd64-kernel-arm_static-23/llvm23.0.0-binutils2.45.1_amd64-kernel-arm_static-20260706.7z
          7za x llvm23.0.0-binutils2.45.1_amd64-kernel-arm_static-20260706.7z
          rm llvm23.0.0-binutils2.45.1_amd64-kernel-arm_static-20260706.7z
      
      - name: Set up ReSukiSU
        working-directory: kernel
        run: |
          echo DATE=$(date +%F) >> $GITHUB_ENV
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash
          cd ./KernelSU
          export KSU_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 30000 "+" 700)
          echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
          sed -i "s/DKSU_VERSION=12800/DKSU_VERSION=${KSU_VERSION}/" kernel/Makefile

      - name: Patch Kernel
        working-directory: kernel
        run: |
          patch -p1 < $GITHUB_WORKSPACE/Patches/ReSukiSU-Manual-hooks.patch
          patch -p1 < $GITHUB_WORKSPACE/Patches/Static-symbol-export.patch
        
      - name: Add Kernel Build Configuration Settings
        working-directory: kernel/arch/arm64/configs
        run: |
          echo "CONFIG_KSU=y" >> ./${{ matrix.devices.config }}

          echo "CONFIG_KSU_MANUAL_HOOK=y" >> ./${{ matrix.devices.config }}
          echo "CONFIG_KSU_MANUAL_HOOK_AUTO_INPUT_HOOK=y" >> ./${{ matrix.devices.config }}
          echo "CONFIG_KSU_MANUAL_HOOK_AUTO_SETUID_HOOK=y" >> ./${{ matrix.devices.config }}
          echo "CONFIG_KSU_MANUAL_HOOK_AUTO_INITRC_HOOK=y" >> ./${{ matrix.devices.config }}

          # Add BBR
          echo "CONFIG_TCP_CONG_ADVANCED=y" >> ./${{ matrix.devices.config }}
          echo "CONFIG_TCP_CONG_BBR=y" >> ./${{ matrix.devices.config }}
          echo "CONFIG_NET_SCH_FQ=y" >> ./${{ matrix.devices.config }}
          echo "CONFIG_TCP_CONG_BIC=n" >> ./${{ matrix.devices.config }}
          echo "CONFIG_TCP_CONG_CUBIC=n" >> ./${{ matrix.devices.config }}
          echo "CONFIG_TCP_CONG_WESTWOOD=n" >> ./${{ matrix.devices.config }}
          echo "CONFIG_TCP_CONG_HTCP=n" >> ./${{ matrix.devices.config }}
          echo "CONFIG_DEFAULT_TCP_CONG=bbr" >> ./${{ matrix.devices.config }}

          if [ "${{ inputs.enable_feature_kpm }}" = "true" ]; then
            echo "CONFIG_KPM=y" >> ./${{ matrix.devices.config }}
          fi
      
      - name: Build Kernel for ${{ matrix.devices.name }}
        working-directory: kernel
        env:
          args: |+
            LLVM=1 ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi- CC=clang LD=ld.lld
            HOSTLD=ld.lld O=out KCFLAGS+=-O2 KCFLAGS_PERF+=-O3
        run: |
          export KBUILD_BUILD_TIMESTAMP=$(date)
          export PATH=/usr/lib/ccache:$GITHUB_WORKSPACE/toolchain/bin:$PATH
          make -j$(nproc --all) ${args} ${{ matrix.devices.config }} all

      - name: Make AnyKernel3
        run: |
          ls $GITHUB_WORKSPACE/kernel/out/arch/arm64/boot/
          cp $GITHUB_WORKSPACE/kernel/out/arch/arm64/boot/Image $GITHUB_WORKSPACE/AnyKernel3
      
      - name: Pack kernel image with AnyKernel3
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.devices.name }}_ReSukiSU_${{ env.KSUVER }}
          path: AnyKernel3
          compression-level: 9
